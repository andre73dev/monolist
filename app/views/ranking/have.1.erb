<div class="container">
  <div class="container-inner inner-default clearfix">
    <% @items.each_with_index do |item,i| %>
      <div class="item-box">
        <div class="image">
          <div class="img-wrap">
            <% if item.medium_image.present? %>
              <%= i+1 %><%= image_tag(item.medium_image , alt: item.title) %>
            <% else %>
              <%= image_tag("no-image.png" , alt: item.title) %>
            <% end %>
          </div>
        </div>
        <div class="detail">
          <p class="title">
            <%= item.title %>
          </p>
        </div>
      </div>
    <% end %>
  </div>
</div>